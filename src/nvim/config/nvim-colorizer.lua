-- [[ Colorizer Setup ]]

require'colorizer'.setup {

}

-- [[ Available Commands ]]

-- Check if some commands has changed
-- :help nvim-colorizer-commands

-- ColorizerAttachToBuffer      -- Should be keybound so we can activate it on files without extension
-- ColorizerDetachFromBuffer
-- ColorizerReloadAllBuffers    -- Should be called if we make changes on setup
-- ColorizerToggle              -- Should be keybound if we wish to toggle it on/off