version: "3.8"
## See https://github.com/compose-spec/compose-spec/blob/master/spec.md

## Templates
x-build-args:
  &default-my-build-args
  ALPINE_TAG: ${ALPINE_TAG}
  DEV_USER: ${DEV_USER}
  DEV_UID: "${DEV_UID:?'Missing DEV_UID Run DEV_UID=$(id -u) DEV_GID=$(id -g) docker-compose build [options]'}"
  DEV_GID: "${DEV_GID}"

x-build-args-xdg_x:
  &default-build-args-xdg-x
  STD_XDG_DATA_HOME:
  STD_XDG_CONFIG_HOME:

x-base-developer:
  &default-base-developer
  build:
    context: .
    dockerfile: Dockerfile-base-developer
    args:
      << : *default-my-build-args
  image: ${ALL_DOCKER_REGISTY_AND_PATH:-}base-developer:latest

x-tmux-developer:
  &default-tmux-developer
  build: 
    context: .
    dockerfile: Dockerfile-tmux
    #target: toolchain
    #target: tmux-build
    args:
      << : *default-my-build-args
      << : *default-build-args-xdg-x
      TMUX_RESURRECT_PROCESSES: "${TMUX_RESURRECT_PROCESSES}"
  hostname: tmux-docker
  ports:
    - '2022:22'

x-nvim-developer:
  &default-nvim-developer
  build:
    context: .
    dockerfile: Dockerfile-nvim
    #target: toolchain
    #target: neovim-build
    args:
      << : *default-my-build-args
      << : *default-build-args-xdg-x

## Services
services:
  base-developer_latest:
    *default-base-developer
  base-developer_specific:
    << : *default-base-developer
    image: ${ALL_DOCKER_REGISTY_AND_PATH:-}base-developer:${ALPINE_TAG}
  nvim-developer_latest:
    << : *default-nvim-developer
    image: ${ALL_DOCKER_REGISTY_AND_PATH}nvim-developer:latest
  nvim-developer_specific:
    <<  : *default-nvim-developer
    image: ${ALL_DOCKER_REGISTY_AND_PATH}nvim-developer:${NEOVIM_DOCKER_TAG}
  tmux-developer_latest:
    << : *default-tmux-developer
    image: ${ALL_DOCKER_REGISTY_AND_PATH}tmux-developer:latest
  tmux-developer_specific:
    <<  : *default-tmux-developer
    image: ${ALL_DOCKER_REGISTY_AND_PATH}tmux-developer:${TMUX_DOCKER_TAG}
  # vscode-developer:
  #     build:
  #         context: .
  #         dockerfile: Dockerfile-vscode
  # rider-developer:
  #     build:
  #         context: .
  #         dockerfile: Dockerfile-rider
